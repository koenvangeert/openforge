# Decisions — action-simplification

## User Decisions (confirmed)
- Start Task prompt: task's own title/prompt only, no prepended instruction
- Legacy fallback: NULL → sensible defaults (no backfill)
- Permission modes: all 5 including dontAsk with visual danger warning
- Old action JSON: silently ignore agent field
- start_implementation: keep deprecated but untouched
